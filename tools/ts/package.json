{
  "name": "luavts-bench-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TS workloads compiled to both Lua (TSTL) and JS (tsc) for the luavts benchmark.",
  "scripts": {
    "build": "npm run build:lua && npm run build:js",
    "build:lua": "tstl -p tsconfig.tstl.json",
    "build:js": "tsc -p tsconfig.tsc.json"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "typescript-to-lua": "^1.36.0",
    "@typescript-to-lua/language-extensions": "^1.19.0",
    "lua-types": "^2.13.1"
  }
}
